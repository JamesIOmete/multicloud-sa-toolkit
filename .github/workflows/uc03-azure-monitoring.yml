name: uc03-azure-monitoring

on:
  workflow_dispatch:
    inputs:
      storage_account_name:
        description: "Azure Storage account name for Terraform state"
        required: true
      notification_emails:
        description: "JSON list of email addresses for alert notifications"
        required: true
      location:
        description: "Azure region for resources"
        required: false
        default: "westus2"

permissions:
  id-token: write
  contents: read

jobs:
  plan:
    runs-on: ubuntu-latest
    env:
      TF_IN_AUTOMATION: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Prove identity
        run: |
          az account show --query '{subscription:id, tenant:tenantId, user:user.name}' -o jsonc
          echo "ARM_* environment variables should be empty when using azure/login with OIDC:"
          env | sort | rg '^ARM_' || true

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Write backend config
        run: |
          cat <<EOF > backend/azure-uc03-monitoring.hcl
          resource_group_name  = "mcsa-tfstate-rg"
          storage_account_name = "${{ inputs.storage_account_name }}"
          container_name       = "tfstate"
          key                  = "uc03/azure/monitoring-starter.tfstate"
          use_azuread_auth     = true
          EOF

      - name: Terraform Init
        working-directory: use-cases/03-monitoring-starter/azure/terraform/root
        run: terraform init -reconfigure -backend-config=../../../../../backend/azure-uc03-monitoring.hcl

      - name: Terraform Plan
        working-directory: use-cases/03-monitoring-starter/azure/terraform/root
        run: |
          export ARM_SUBSCRIPTION_ID="${{ vars.AZURE_SUBSCRIPTION_ID }}"
          cat <<EOF > /tmp/uc03.auto.tfvars.json
          {
            "notification_emails": ${{ inputs.notification_emails }},
            "location": "${{ inputs.location }}"
          }
          EOF
          terraform plan -var-file=/tmp/uc03.auto.tfvars.json
